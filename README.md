# Emitrr - Backend

### Steps to run this project

1. Clone this repo
2. cd to folder
3. cd to frontend
4. run `npm install`
5. Add .env variables
6. run `npm run dev`
7. cd to backend
8. run `npm install`
9. add .env variables
10. run `npm run dev`
